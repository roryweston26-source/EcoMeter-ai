# OS
.DS_Store
Thumbs.db

# Build output
releases/
*.zip

# Node (if anyone runs scripts locally)
node_modules/
npm-debug.log*

# Editor
.vscode/
.idea/
*.swp

# Claude Code — shared config (.claude/launch.json, static-server.js) IS committed;
# only the per-machine permission allowlist is local.
.claude/settings.local.json

# API keys for scripts/measure-reasoning.js — local only, never committed
scripts/.keys.local.json
